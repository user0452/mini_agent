# 测试与验收

## 自动化测试

运行全部测试：

```powershell
python -m unittest discover -s tests -v
```

测试不调用真实模型 API。测试客户端会返回确定性的 OpenAI 兼容消息，用来验证
Agent 控制流，而不是验证某个模型是否一定选择某个工具。

## 功能覆盖

| 能力 | 自动化场景 |
| --- | --- |
| 直接回复 | 模型没有 `tool_calls` 时立即返回最终答案 |
| 工具 Schema | 三个工具都有名称、描述、对象参数和必填字段 |
| 三工具执行 | 同一个模型步骤调用 calculator、search、read_docs |
| 内部 Loop | 工具结果触发第二个工具调用，之后才返回答案 |
| 循环上限 | 模型持续调用工具时由 `max_steps` 终止 |
| Session 隔离 | 用户 A 的 window-1 不会读取 window-2 内容 |
| 带工具追问 | 重开 window-1 后可读取旧工具结果并调用新工具 |
| 思考展示 | reasoning 回调可见，但不会进入上下文或 SQLite |
| 上下文压缩 | 每五轮独立摘要，完整轮次保持在 10～15 轮 |
| API 重试 | 网络错误指数退避、随机抖动、最多三次 |
| 工具 Trace | 成功/失败、耗时、模型步骤、脱敏和 SQLite 持久化 |
| 文档边界 | 可读取已放入 docs 的 md/txt，拒绝目录穿越 |

## 文档能力边界

`read_docs` 不是上传功能。自动化测试会把测试文档预先写入临时 `docs` 目录，再让
Agent 调用 `read_docs`。当前没有 HTTP 上传接口、Web 文件选择器或 `/upload`
命令，因此测试不会宣称支持文档上传。

## 真实模型验收建议

配置 OpenAI 兼容 API 后，手动运行以下场景：

```text
1. 直接问：你好，请介绍一下你自己。
2. 工具问：计算 (18 + 7) * 4。
3. 连续工具：搜索 minimal agent，然后计算 6 * 7。
4. 文档问：读取 intro.md 并总结。
5. Session：分别用 window-1 和 window-2 交叉追问，确认历史不串线。
6. Trace：完成工具调用后输入 /trace。
```

模型的工具选择具有概率性，因此真实模型测试适合作为集成验收，不适合替代确定性
单元测试。
